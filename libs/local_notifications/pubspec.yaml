name: local_notifications
description: Shared local OS / in-session browser notification scheduling for Flutter product apps.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^19.0.0
  flutter_timezone: ^4.1.0
  shared_preferences: ^2.5.3
  timezone: ^0.10.0
  web: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
